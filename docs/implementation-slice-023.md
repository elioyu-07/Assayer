# Vertical Slice 023: Generic Real-URL Host Acceptance (B12)

B12 connected URL startup, bounded exploration, Host identity binding, observation Case, safe action, Evidence, recovery, decision preparation, commit, and completion into a deterministic smoke runner. Lease-mock is an external black-box sample; no domain, route, labels, or selectors are embedded.

Smoke validates Host facts only and cannot claim LLM semantics. Missing controls, query/reset semantics, or unique list binding produce explainable needs_review; unsanitized Raw Visual cannot become issue_found. Formal coverage after C02 uses attempted/resolved/unresolved Findings.
