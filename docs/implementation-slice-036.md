# Vertical Slice 036: End-to-End User Delivery Closure (C07.3-01-05)

This slice defines delivery by the user journey: URL -> Codex Skill/MCP -> real Chromium -> result and artifacts. CLI keeps assayer audit <url> but must not nest Codex. Product MCP generates internal envelopes, exact schemas, fixed Alpha parameters, current Scan/Run/revision, and Operation references; users and models provide business input and public rationale only.

Host guards navigation before stale Cases, supports atomic finding/prepare/commit, and derives completion coverage from the ledger. Lease heartbeat runs on the MCP/Playwright thread; EOF or close fails active Scans immediately. New-session acceptance is required after Plugin changes; no silent smoke fallback.
