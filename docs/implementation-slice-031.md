# Vertical Slice 031: LLM Browser Observation Closure (C07.1)

Adds read-only observe_page for the current verified object. Host returns filter/object bounds, controls, deduplicated logical lists, columns, rows, spatial relationships, relation edges, and same-viewport runtime_visual Evidence. MCP attaches the immutable PNG; JSON and ledger store metadata and references only.

Pagination, navigation, tabs, and menus are excluded from business lists. Binding review requires observe_page visual/logical-list Evidence, not pageListCount alone. FUA-10 remains frontend-only and B07c remains enforced.
