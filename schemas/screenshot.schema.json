{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://assayer.dev/schemas/screenshot.schema.json",
  "title": "Screenshot",
  "type": "object",
  "additionalProperties": false,
  "required": ["screenshotId", "scanId", "pageStateRef", "objectRef", "kind", "status", "capturedAt", "capturedAtRevision", "sanitizationPolicyVersion", "sanitizationStatus"],
  "properties": {
    "screenshotId": { "$ref": "common.schema.json#/$defs/entityId" },
    "scanId": { "$ref": "common.schema.json#/$defs/entityId" },
    "pageStateRef": { "$ref": "common.schema.json#/$defs/entityId" },
    "objectRef": { "$ref": "common.schema.json#/$defs/entityId" },
    "caseRef": { "$ref": "common.schema.json#/$defs/entityId" },
    "kind": { "enum": ["raw_visual", "issue"] },
    "status": { "enum": ["captured", "not_located", "ambiguous", "rejected"] },
    "capturedAt": { "$ref": "common.schema.json#/$defs/timestamp" },
    "capturedAtRevision": { "$ref": "common.schema.json#/$defs/runRevision" },
    "sanitizationPolicyVersion": { "$ref": "common.schema.json#/$defs/algorithmVersion" },
    "sanitizationStatus": { "enum": ["sanitized", "not_performed", "failed"] },
    "rawVisualRef": { "$ref": "common.schema.json#/$defs/entityId" },
    "path": { "$ref": "common.schema.json#/$defs/nonEmptyString" },
    "digest": { "$ref": "common.schema.json#/$defs/sha256" },
    "imageType": { "enum": ["png", "jpeg", "webp"] },
    "width": { "type": "integer", "exclusiveMinimum": 0 },
    "height": { "type": "integer", "exclusiveMinimum": 0 },
    "problemBoundingBox": { "$ref": "common.schema.json#/$defs/boundingBox" },
    "sourceBoundingBox": { "$ref": "common.schema.json#/$defs/boundingBox" },
    "annotation": { "$ref": "common.schema.json#/$defs/nonEmptyString" },
    "failureReason": { "$ref": "common.schema.json#/$defs/reason" }
  },
  "allOf": [
    {
      "if": { "properties": { "status": { "const": "captured" } }, "required": ["status"] },
      "then": { "required": ["path", "digest", "imageType", "width", "height", "problemBoundingBox", "sourceBoundingBox", "annotation"] },
      "else": { "required": ["failureReason"] }
    },
    {
      "if": { "properties": { "kind": { "const": "issue" } }, "required": ["kind"] },
      "then": { "required": ["rawVisualRef", "problemBoundingBox", "annotation"] }
    }
  ]
}
