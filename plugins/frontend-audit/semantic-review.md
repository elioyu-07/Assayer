# Frontend filter review

Review the current frozen browser snapshot for each declared dimension.

Mark a dimension `satisfied` only when the snapshot provides direct evidence.
Mark it `violated` when the evidence shows a concrete usability or binding
defect. Use `unresolved` when the snapshot is insufficient. A complete result
with all dimensions satisfied is ready; any violated dimension is rework.
